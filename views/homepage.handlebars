<h1>Welcome to Tech Blog</h1>
{{#each blogs as |blog|}}
    <article>
        <h2>{{blog.title}}</h2>
        <p>{{blog.content}}</p>
        <!-- Add other blog details here -->
    </article>
{{/each}}
